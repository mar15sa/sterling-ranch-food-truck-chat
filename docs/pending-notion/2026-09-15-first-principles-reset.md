# Pending owner-guide update: first-principles reset

Proposed September 15, 2026. The latest trace shows that the assistant can lose resident-request facets before retrieval, then call the resulting partial answer complete. Broad model selection is frozen. The approved direction is a need-first contract that preserves every requested outcome through evidence selection, writing, validation, and rating.

Implemented locally: new question-log records publish `Not rated` unless a versioned assessment has explicitly passed human calibration. This prevents the known uncalibrated heuristic from presenting confident positive grades. It is not released or verified live.

Implemented locally in shadow mode: the need contract replays all 12 latest captured questions without paid calls and preserves the compound truck/menu, recycling/storage, pool status/hours, and shed/form requests as separate needs. It carries only resident-authored context into dependent yoga and lighting follow-ups. Returned sources are assigned as candidates per need, but only relevant rendered verified claims count as support. Each need is labeled supported, missing-evidence, conflict, ambiguous, or unassessed; unassessed is required when a legacy answer has no claim-to-source map.

Replay result: 2 complete, 5 missing-evidence, 4 unassessed, and 1 ambiguous. The check supports the food-truck identity while flagging the missing menu, rejects the unrelated water-billing contact for the CAB 2025 water-quality report, and treats pool status and regular hours as separate missing needs. Two hundred direct compatibility checks pass. No model or database call, resident question, source approval, subscription, setting change, release, or live behavior change occurred. There is no new recurring cost commitment.

Next proposed local slice: make retrieval and answer composition consume the need contract behind a test-only or shadow gate. Preserve supported partial results, state unresolved needs clearly, and offer only need-relevant next steps. Re-run the frozen capture and negative controls before considering resident rendering. Keep model/vector/fine-tuning work deferred until a bounded need-first failure demonstrates that one would add meaningful quality relative to its full operating cost.

Target updates when external synchronization is authorized: add the local shadow stage and state labels to the accessible answer-flow explanation and diagram on [How the project works](https://www.notion.so/3dabf909186d8166b507c2a4e1d1aced); record the need-first architecture decision and its no-new-cost status on [Decisions and their reasons](https://www.notion.so/3dabf909186d8139ac52ebdbf77d8bea). Label both implemented locally, not released or verified live, and cite the local commit plus `docs/COMMUNITY-FIRST-PRINCIPLES-RESET-2026-09-15.md`. Fetch both pages before editing and after editing to preserve unrelated content and verify the update.

Additional local implementation: a `shadow-route` coordinator now runs at most four independently supplied need answers, validates each need separately, contains individual failures, preserves supported partial answers, assigns source need IDs, and selects only need-relevant next actions. The mode requires `isTest: true` plus an explicit per-need answer function; it is not configured on the server and leaves the current answer unchanged. Ten focused router checks pass. The complete affected set is 210 passing direct checks.

The saved legacy replay produces 2 complete, 1 verified partial, 4 missing-evidence, 4 unassessed, and 1 ambiguous. It correctly turns food truck/menu from globally verified into a useful partial result and excludes the wrong CAB-report billing action. Replay limitation: it rechecks the same captured response per need, so it proves isolation/composition but not new retrieval quality. The next local slice must connect the real existing source and live adapters under test mode. There is still no release, live verification, model/database call, subscription, or new recurring cost.

Source record: `docs/COMMUNITY-FIRST-PRINCIPLES-RESET-2026-09-15.md`.

Further local implementation: the test-only router now connects the existing community index, rules engine, food-truck/menu adapter, pool-status adapter, and waste-calendar adapter once per preserved need while every model stage is disabled. Real-path fixtures complete food truck/menu, recycling date/bin storage, and water payment; pool status/hours becomes a useful verified partial; lighting remains explicitly unassessed because its legacy rules result lacks a claim-to-source map. Live waste answers now retain their evidence mapping, schedule clauses are classified independently, equivalent date formats verify correctly, and composition selects the relevant sentence rather than copying the full source passage.

Cost state: $0 in new model API calls or subscriptions. The preserved historical diagnostic benchmark estimates the current mixed model flow at $1.01 per 1,000 questions for its sample ($10.09 at 10,000), while the tested need-first path disables model calls. Its current shadow harness performs the original combined pass plus one pass per need, so a two-part test makes three connector-path attempts. A production candidate must replace the combined pass and deduplicate connector capabilities before total hosting, latency, and monthly cost can be quoted. No model or vector service is selected.

Verification: 28 focused need-contract/router tests pass, with 145 broader assistant/completion/interpretation checks also passing. One unrelated completion expectation differs on contact-boundary versus freshness-withheld behavior and was not caused by this slice. No resident behavior, rating, source approval, configuration, deployment, or live state changed.

Next local slice: expose structured claim-to-source mappings from the legacy rules boundary, then deduplicate connector calls by capability. Keep both implemented locally only until an end-to-end offline comparison measures useful complete/partial answers, latency, all calls, and current-versus-candidate monthly cost.

Pending September 15 addendum for [How the project works](https://www.notion.so/3dabf909186d8166b507c2a4e1d1aced), answer-flow explanation and diagram: label this **implemented locally and verified in tests; not released or verified live**. The deterministic rules boundary now maps only rendered claims that preserve the governing source's subject, permission, dates, and numbers. The need verifier rejects changed permissions, measurements, and wrong-subject sources. The test-only route now completes the permanent-seasonal-lighting follow-up, separates shed height from the official shed form, and preserves yoga through a dependent “tomorrow” follow-up with event-specific date/time proof. Identical connector requests are reused within one shadow run: food-truck and pool fixtures reduce three internal requests to one actual connector call plus two cache hits; yoga reduces two requests to one call plus one cache hit. The diagram should show the remaining baseline-plus-per-need duplication as local experimental scaffolding, not the intended production flow.

Pending September 15 addendum for [Decisions and their reasons](https://www.notion.so/3dabf909186d8139ac52ebdbf77d8bea): record that no model, reranker, or vector database is selected. The measured failures in this slice were evidence attribution, route ordering, subject preservation, date/time equivalence, and duplicate connector execution; deterministic repairs resolved them. Adoption of another component remains conditional on a frozen benchmark showing a meaningful gain for a remaining isolated failure with full cost and latency.

Evidence to cite: local implementation commit `80d775c` on `codex/community-quality-september30`, containing `lib/community-request-contract.js`, `lib/community-need-router.js`, `lib/community-assistant.js`, `lib/rules-assistant.js`, `test/community-need-router.test.js`, and `test/rules-evidence-claims.test.js`, plus `docs/COMMUNITY-FIRST-PRINCIPLES-RESET-2026-09-15.md`. Verification is 200/200 focused regression checks, 0 failures. Cost added in this slice is $0 in model/API calls and $0 in subscriptions. The preserved historical current-flow estimate remains $1.01 per 1,000 and $10.09 per 10,000 questions for the recorded diagnostic mix. No resident question, external source request, source approval, setting change, deployment, or live verification occurred.

Synchronization remains pending because the earlier Notion write attempt was blocked by automatic approval review. Fetch each target page before editing and after editing, preserve unrelated owner content, insert the local commit link once available, and keep the status below verified-live until the deployed revision is checked.

Pending September 15 production-shaped candidate addendum for [How the project works](https://www.notion.so/3dabf909186d8166b507c2a4e1d1aced): label this **implemented and verified locally; not released or verified live**. The test-only answer flow now runs one need-first coordinator, 0 old combined baseline runs, one local source path per preserved need, shared connector reuse, human-first composition, and a second evidence check against the exact displayed answer. Sibling route requests carry only the needed resident-authored subject context. Displayed supplemental facts retain their own source IDs, omitted facts cannot count toward completion, and scoped claims that an inspected source omits an exact clock time are invalidated when that source contains one.

Pending September 15 decision addendum for [Decisions and their reasons](https://www.notion.so/3dabf909186d8139ac52ebdbf77d8bea): the candidate passes 19/19 offline cases: 12 authored production-shape fixtures plus all 7 pre-existing frozen rule holdouts. Results are 13 complete, 3 correctly withheld/clarified, 3 handled safety or scope boundaries, and 0 claim-proof failures. Execution was 19 coordinator runs, 0 baseline runs, 21 per-need runs, 2 safety-boundary runs, 0 model calls, and $0 added model/API cost. The final recorded local p95 was 1.952 seconds. The wider regression is 207/207 passing.

Cost note for the decision record: the preserved historical current-flow diagnostic estimate remains about $1.01 per 1,000 questions and $10.09 per 10,000. The tested candidate adds $0 in model/API charges because all model stages are disabled. This is not a full production operating-cost estimate; hosting and real connector work remain to be measured after integration. No model, reranker, vector database, subscription, or recurring spend is selected.

Limitations to preserve in Notion: the 19-case set is not the final broad human-rated acceptance set. The current September 15 pool-hours source is stale, so the current-date candidate correctly withholds regular hours while retaining live status; a September 14 fixture tests the routing only within the older source's approved freshness window. Remaining work is current-source review, broad human-rated acceptance, rating calibration, production integration, live latency/cost measurement, and only then a bounded component comparison if a specific failure remains.

Evidence to cite: local implementation commit `28590e5` on `codex/community-quality-september30`, `scripts/eval-community-need-first-offline.js`, and `docs/COMMUNITY-FIRST-PRINCIPLES-RESET-2026-09-15.md`. No resident question, network request, source approval, deployment, or live verification occurred.


Pending September 15 broad-diagnostic addendum for [How the project works](https://www.notion.so/3dabf909186d8166b507c2a4e1d1aced): label this **implemented and verified locally; not released or verified live**. The need-first candidate now recognizes concrete resident topic fragments, preserves proof through safe rule formatting and date/fee headings, shows verified qualifications without using unrelated neighboring claims, handles explicit open/access actions, and ties proactive forms and one-sheets to their own sources.

Pending September 15 decision addendum for [Decisions and their reasons](https://www.notion.so/3dabf909186d8139ac52ebdbf77d8bea): the existing authored diagnostic has improved from 96/151 to 122/151, with 113 completed answers, 38 safe nonanswers, 0 claim-proof failures, 0 model calls, and $0 added model/API cost. The focused production-shaped gate remains 19/19, including all 7 frozen rule checks, and all 50 focused need/router/evidence checks pass.

Preserve these limitations: this 151-phrasing set is authored coverage, not unseen or human-rated acceptance. The previous 116–120 range came from shared process state. The harness now runs every question in its own worker isolate, terminates each worker after its result, and treats any ellipsis inside a source claim as a clipped presentation failure. The prior checkpoint repeated at 119/151 with fingerprint `62202de281ec2a787e527359e72b8c658a17c25887215defb209b925f1fd1b25`. The plain-language composer checkpoint completed once at 122/151 with fingerprint `ac7ae3f621bbf60ebff6ee82e43204d61ffce9a628ea7516466647c354c599a2`; it was not repeated because the local machine was contended and the run was deliberately capped. Of 29 saved expectation failures, 26 complete with proof but need human review for usefulness, breadth, source ordering, and proactive context. The three safe nonanswer/expectation cases are the unreviewed clubhouse source version, trash timing versus location mismatch, and absent current Labor Day-specific pool hours. Earlier isolated p95 was 1.148 and 1.146 seconds; the contended later run was 11.097 seconds and is not a production latency measurement.

Pending rating note: a frozen check incorrectly rejected “72-hour limit” because it required the exact text “72 hours.” Literal checks now tolerate harmless hyphen and singular/plural differences while preserving the factual requirement. Pending answer example: the car-cover response changed from a raw `(b)(18)` legal fragment to a direct verified explanation that a general tarp does not qualify, DRC approval is required, the cover must be neutral-colored, maintained, and made for the vehicle, and separate street-parking rules still apply.

Cost decision: no model, reranker, embedding service, vector database, subscription, or recurring spend is selected. The tested candidate adds $0 in model/API charges. The preserved historical mixed-model estimate remains about $1.01 per 1,000 and $10.09 per 10,000 questions. Test another component only against a privacy-reviewed human-rated failure group that isolates the capability it could improve.

Synchronization remains pending because the earlier Notion write was blocked by automatic approval review. Fetch each target page before editing, preserve unrelated content, and keep the state below verified-live until the deployed revision is checked.

Pending September 15 verified-detail-retention addendum for [How the project works](https://www.notion.so/3dabf909186d8166b507c2a4e1d1aced): label this **implemented and verified locally; not released or verified live**. The answer flow now retains useful verified conditions when resident-friendly wording differs from the legal phrasing, while still checking numeric values and permission meanings. It also carries the obligation from a matching numbered-list heading into the selected list item and prefers a concrete requested measurement over a generic lead sentence.

Pending decision addendum for [Decisions and their reasons](https://www.notion.so/3dabf909186d8139ac52ebdbf77d8bea): all four privacy-screen phrasings and all five vegetable-garden phrasings now pass. The garden answer includes DRC review, rear/side-yard placement, and the five-foot property-line setback. A fence regression found by the production-shaped gate was fixed so the 54-inch measurement remains visible. The 19-case gate passes with zero proof failures.

The single capped isolated broad run reached 125/151 from the prior 122/151: 111 complete, 40 safe nonanswers, zero claim-proof failures, zero model calls, and $0 added model/API cost. Fingerprint: `faea5c89e5182a82e2f4887104f8f1d5e457fb4db4eedf95886210fd8664e1b4`. Do not present 125 as the exact total for the small post-run numeric-equivalence correction; the broad run was deliberately not repeated. Targeted affected families and the focused gates were checked afterward.

No model, reranker, embedding service, vector database, subscription, deployment, or resident setting was added. The historical mixed-model diagnostic remains about $1.01 per 1,000 questions, while this tested path added $0 in model/API charges. This is local engineering evidence, not human-rated acceptance or demo readiness. External synchronization remains pending the earlier automatic approval-review rejection; fetch each page before a later authorized edit, preserve unrelated content, and fetch afterward.

Implementation evidence for this addendum: local commit `5f95048` on `codex/community-quality-september30`.

Verification addendum: 45/45 focused need/router/evidence checks pass. The full repository suite passes 1,307/1,308 checks. The only failure is the previously known diagnostic-label mismatch for conflicted internet-contact evidence; the old number remains withheld. Three regressions first exposed during this slice were repaired before the checkpoint.
