# Community and governing-rule retrieval comparison

Planned September 15 UTC / September 14 Denver. Previous turn made progress: connected approved community semantic selection to experimental packets, exposed silent source-limit omissions and reproduced missing governing evidence plus unrelated neighbors.

The current rules index has 955 loaded documents and 926 eligible documents. A fresh audit finds the eligible corpus exactly matches the earlier corrected semantic cache: hash 652d65cc3f42f7fdad1495d39480a3204a1ade79c1b55ab73d9697b09867c7cb, no added or changed eligible documents. Reuse artifacts/quality-eval/semantic-identity-recapture-20260914 only after the existing full corpus/model/window binding checks pass. Do not revive failed preliminary caches or re-embed unchanged documents.

Freeze four arms on the same five preserved plans and two repetitions, with randomized arm/case order (40 packets): (1) keyword rules and community, (2) keyword rules with community semantic, (3) rule hybrid with community semantic, (4) rule semantic with community semantic. Hybrid uses existing reciprocal-rank fusion unchanged. This adds a pure semantic option without changing default rules-ranker behavior. No topic aliases, thresholds, query changes, new interpretation or writer/checker calls.

Predeclared evidence inspection: in both pergola plans, does the exact governing subsection (b)(44) enter the final packet? In the compound plan, does (b)(48) for hot tubs survive as well? In shed, do (b)(9) and the approved architectural form survive? In lighting, do the current replacement lighting policy and form survive? Compare source omissions and unrelated neighbors, not just raw target counts. The generic application query is a useful unchanged control but does not identify a project type. Do not infer final-answer usefulness from source presence.

Keep the existing 12-source / 30,000-character packet bound, full approved projected facts, complete eligible rule sections, explicit omitted-source accounting, per-need provenance and live routing. Scope, version, lifecycle, current approval and tenant constraints still apply. Models and vectors are already local; disable remote model downloads. Record resource/timing measurements, with zero paid API calls and no new subscriptions. This desktop harness loads two model instances; its memory is not a proposed production architecture estimate.

Current rule content is loaded locally with supplements, not newly attested against live publication. Known development plans are not unseen tests. Human rating calibration, paid writer/checker comparison and full-answer/monthly costing remain incomplete. Notion synchronization remains pending the prior automatic approval review rejection; preserve the exact local update. No source approval or production release is authorized by this experiment.

## Completed result

Capture **a97fccf4dfda7233203978cb0124ec010f56e558**, artifacts/quality-eval/dual-semantic-retrieval-20260915. All 40 packets completed; process 1282 exited 0. Twenty-seven focused tests passed before execution. The two control arms return source payloads identical to the prior accounted comparison. All 20 case/method pairs are stable across both repetitions.

| Governing-rule / community search | Unique cases with all declared diagnostic sources | Important change |
| --- | ---: | --- |
| Keyword / keyword | 1/5 | Baseline missing forms and pergola rule |
| Keyword / semantic | 3/5 | Form gains; pergola rule still absent |
| Hybrid / semantic | 4/5 | Pergola rule recovered in both plans; shed form dropped |
| Semantic / semantic | 4/5 | Same diagnostic coverage tradeoff; different irrelevant neighbors |

These targets are specific necessary sources, not a complete-answer rubric: architectural form plus shed rule; architectural form plus current lighting policy; hot-tub plus pergola rules; pergola rule plus form; and a navigation source for generic application lookup. In particular, finding the lighting policy and form does not establish every required process source. The table does not mean 80% useful answers, independent unseen success, statistical significance or human calibration.

Both combined methods recover the full (b)(44) pergola subsection with requirements intact, confirmed by inspecting actual packet text. The hot-tub (b)(48) rule remains in the compound packet. No rule was newly approved or retrieved from a different tenant/version. However, the shed packet now has 13 candidate units, and its architectural form is last: the 12-source budget omits it in both combined methods. The omission record identifies the exact form and form need. This is a selection regression, despite improved overall diagnostic source coverage. Keep the result unselected for production.

Irrelevant material remains: lighting includes the monthly-fee chart and streetlight reporting; shed includes pickleball reservation and water-use references; pergola includes internet support and unrelated rules. Pure semantic rules also supply plant-list, sunshade and roof-form sections. The compound packets each omit ten of 22 candidates. Raising the packet bound would admit more irrelevant material as well as the form. Final relevance and need coverage must be considered before applying the limit; retrieving more text alone does not solve the problem.

## Measured cost and timing

No paid API calls, subscriptions, model downloads, new embedding builds or deployment. The exact current 926-document rule corpus reuses the corrected 1,569-window vector index. The community index is unchanged. Both use the previously tested BGE model and revision.

| Search configuration | Packet retrieval median / sample p95, ten attempts |
| --- | ---: |
| Keyword / keyword | 2,928.5 / 4,095 ms |
| Keyword / semantic | 2,358 / 3,955 ms |
| Hybrid / semantic | 3,290.5 / 5,726 ms |
| Semantic / semantic | 633 / 1,261 ms |

The pure semantic arm avoids the rules keyword pass; it was faster in this randomized local capture. This small experiment does not establish a production p95 or whole-answer time. Source expansion, identity/freshness checks and packet construction are included. Context size varies by question/method and is preserved in comparison.json; character counts are not billed tokens.

Community initialization took 1,414 ms; rule-model initialization/window verification took 2,930 ms. Peak process memory was 454,909,952 bytes (about 434 MiB) for a harness with two model instances. Production hosting and concurrency remain unmeasured, not free. Existing model-call cost ledgers are unchanged; no full-answer or monthly price is selected from these retrieval-only results.

## Next controlled option

Test a learned second-stage relevance scorer before the final packet limit. It should compare each question/need with candidate evidence, retain approval/freshness/tenant gates, keep action-navigation context separate from fact approval, preserve full selected evidence, and expose all rejected/omitted sources. Freeze candidate sets and compare against the captured current ordering; never insert the known target IDs into model inputs or use tuned topic exceptions. A score is a ranking aid, not a permission, fact-validation or calibrated answerability probability. Keep negative controls and compound coverage in the next evaluation; do not let one subject consume every slot.

Primary documentation checked September 15 UTC: [Sentence Transformers cross-encoder usage](https://sbert.net/docs/cross_encoder/usage/usage.html) describes scoring query/passage pairs and reranking a retrieved set, with extra per-pair computation. [The Xenova MiniLM reranker model card](https://huggingface.co/Xenova/ms-marco-MiniLM-L-6-v2) provides a Transformers.js sequence-classification example using text pairs and quantized weights. This makes a local, no-API candidate feasible to investigate; it does not prove it helps this assistant. No new model has been downloaded or adopted yet. Pin and verify any artifact and measure cost/latency before integrating it.

Reproduce the report: `node scripts/quality-eval/summarize-dual-semantic.js artifacts/quality-eval/dual-semantic-retrieval-20260915 artifacts/quality-eval/combined-retrieval-accounted-20260915`. The cache identity audit is artifacts/quality-eval/current-rules-cache-audit-20260915/audit.json. Exact Notion update remains pending locally. Final-answer comparisons, automatic rating calibration and live demo validation are still required.
