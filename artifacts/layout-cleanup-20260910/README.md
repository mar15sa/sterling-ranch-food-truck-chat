# Resident page cleanup

Owner request: remove email-draft explanations everywhere, the redundant Today prefix, repeated Community Assistant heading, and space-consuming sidebars.

Cause: repeated template copy and promotional aside columns constrain the resident tools. The homepage truck title will display its API-provided name without a prefix; the Assistant heading will ask "How can we help?" below its category label. Assistant, Food Trucks, Calendar and Pool will use the full content width after removing their promotional sidebars. Feedback links and answer-level source controls remain functional.

This is owner-authorized presentation work. No Assistant question family, evidence authority, claim approval, freshness, connector, adapter, routing or answer-generation behavior changes. Existing data failure notices and official links within the tools remain intact.

Verification: all 12 desktop/mobile page checks passed, including 12 full-page WCAG A/AA scans, full-width content, absent deleted copy/asides, live truck title, source-details open/Escape, navigation, and no overflow or page errors. Zero Assistant requests were submitted. Screenshots and preview-results.json record the candidate rendered against staging data. CLEANUP_LIVE=1 repeats the checks against the deployed pages. JavaScript syntax and whitespace checks passed.

Openings now retains its count and last-updated information inline below the introduction; its former side panel is removed. Its map, filters, listing data and tip form are unchanged.
