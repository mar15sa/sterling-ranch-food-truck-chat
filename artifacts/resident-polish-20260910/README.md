# Resident polish after audit follow-up

Owner requested footer clipping correction, Calendar partial notice below the list, and recently opened places first. Pool homepage replacement is a preference discussion pending the owner's selection.

Root cause/family: a viewport-height media query hides chat navigation/footer on short desktop windows; the compact footer renders a 150px botanical background inside a roughly 62px area. Correct shared footer sizing/art and limit keyboard-specific hiding to focused mobile composition. Preserve transcript-only scrolling and stable composer above the footer in normal desktop windows. Presentation applies to all Assistant questions, not individual wording.

Calendar notices move below all listings without weakening their text or official link. Openings retains every status and source record, with open places ordered first in cards, category controls, and the homepage preview. Sorting does not infer opening dates from verification dates.

Authority/reuse: frontend only; existing source status, evidence, review, profile/adapter, and answer routing remain unchanged. No resident facts are added. Source failures and official actions remain available. Browser tests use visible Test mode with intercepted isTest responses for automated Assistant questions. Check footer descendant bounds at short/narrow desktop widths, chat scrolling/composer positions, calendar partial placement, and Openings ordering/filter access before and after exact staging deployment.

The owner confirmed both weather and resident essentials. Weather configuration uses Sterling Ranch coordinates published at https://sterlingranch.com/happenings/news/sterling-ranch-a-top-ranked-master-plan-oasis-in-colorado/ and confirmed by the NWS /points response (relativeLocation city: Sterling Ranch). NWS API contract: https://www.weather.gov/documentation/services-web-api . Weather uses a separate reusable read-only service with coordinate/contact configuration, not Assistant answer routing. Cache: 15 minutes, shared concurrent reads; errors cached for one minute. Responses older than 24 hours or without future periods fail closed to the official forecast link. No weather facts are embedded in code. The homepage renders named forecast periods, not an inferred current observation. Resident essentials preserve the existing Assistant and Pool destinations.

Candidate release checks passed: all six pages at 1440×900, 1024×768, 800×700, 1440×540, 390×844, and 320×740 (36 combinations). Footer visible and descendant bounds inside its area, no horizontal overflow, shared feedback links, chat input visible and transcript scroll independent of composer. Axe WCAG A/AA checks reported no violations. Calendar partial notice last, earlier/unknown times and source failures preserved. Openings first category/control and first card are recently open; all categories, pagination and filters remain accessible. NWS live lookup confirmed Sterling Ranch; weather cache/concurrency/expiry/stale-source/host/failure fixtures passed. No test questions were sent to the resident log. Native mobile keyboards were not available; browser viewport simulations were used.
