# September quality candidate: current production integration

September 15 UTC / September 14 Denver. Local integration, not a release. The previous goal turn made progress by testing the reasoning configuration, recording the credit failure and discovering a materially newer deployed baseline. Paid model tests remain stopped; this work uses no provider generation.

## Problem and intended outcome

Historical experiments used the earlier source library and coordinator. Production health now identifies d04fb2db699a73c61ee2d7a047d7ef90fd5aea1f, which includes scoped form/process sources and shared request/source-boundary corrections. Comparing a candidate against an obsolete library could recommend extra AI to compensate for a gap already addressed. Bring that exact production revision into the isolated quality branch before measuring the current baseline. Keep historical captures immutable and retain all candidate fixes, including unresolved-subject clarification, source identity, source expiry, complete rule context, calendar filters and community-local dates.

Before: the experimental candidate has neither the full current reviewed source package nor all its retrieval/coverage controls. Intended after: the candidate contains both the current production source boundary and earlier shared quality fixes. This integration alone does not establish better resident answers or a winning model. Relevant families include form/process requests, multi-part rules, report findings, current operations, ordinary hours/booking, ambiguous follow-ups and calendar filters.

## Authority and reuse

Import exact existing source decisions, versions, quoted claims and actions without expanding approvals or altering withheld claims. Adopted rules remain authoritative for binding requirements; reviewed process records and forms support only their own allowed steps/actions; live connectors retain their current operational scope and failure behavior. Community-specific facts stay in the existing profiles and source records. The merge changes no source approval, model setting, subscription or deployment.

Only three textual overlaps required resolution: union of contract/conversation imports in community-assistant, union of interpretation helper exports, and union of search helper exports. Both branches' implementations remain present. Automatically merged selection and expiry behavior must still pass the combined tests. The earlier source lifecycle gate and the newer canonical future-expiry gate are complementary; neither is removed simply because both check freshness.

The 147-file production delta includes previously released private owner UI changes; these are imported as existing production behavior, not new design proposals. No additional resident-facing UI change is made here. Preserve the dirty original workspace and untracked experimental capture/cache directories.

## Verification plan and evidence

1. Revalidate eligible approved sources into a separate temporary snapshot using the existing release bridge, without editing the checked-in index or broadening approvals. Initial result: passed, 39 due URLs checked, approved fingerprint 94c0b7d9ab0b3779fbe4009e01f9639b243ae95688e51da04a8077f2ef041bca. Exact attestation and per-source checks are in artifacts/quality-eval/current-production-integration-20260915.
2. Commit the resolved local candidate, then run the existing full release-check wrapper using that explicit snapshot. Capture exact revision, unchanged-runtime check, all test counts and any failures. No model credentials are supplied; model generation is disabled for the local release gate.
3. Diagnose failures by question family and distinguish a merge regression from a stale assertion or fixture. Do not weaken authority/completeness expectations to produce a green result. Verify earlier quality fixes and newly imported source-boundary behavior together.
4. After compatibility, refresh the matched baseline and current source scope before further model comparisons. Paid model comparisons remain blocked by the observed credit rejection. Human calibration, final unseen tests, complete cost comparison and live candidate rehearsal remain outstanding.

No staging/production test questions are submitted by this integration. Any later hosted question must include isTest true; browser testing requires the test-mode URL and visible banner. Production health at 2026-09-15 01:28:11 UTC identifies only the deployed revision/configuration, not current provider credit or answer quality.

Documentation impact: local integration and evaluation-baseline decision. Exact Notion update remains pending the earlier external-write rejection; do not relabel the candidate live or overwrite the other task's source-review documentation.
