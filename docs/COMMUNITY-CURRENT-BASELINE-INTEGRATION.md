# September quality candidate: current production integration

September 15 UTC / September 14 Denver. Local integration, not a release. The previous goal turn made progress by testing the reasoning configuration, recording the credit failure and discovering a materially newer deployed baseline. Paid model tests remain stopped; this work uses no provider generation.

## Problem and intended outcome

Historical experiments used the earlier source library and coordinator. Production health now identifies d04fb2db699a73c61ee2d7a047d7ef90fd5aea1f, which includes scoped form/process sources and shared request/source-boundary corrections. Comparing a candidate against an obsolete library could recommend extra AI to compensate for a gap already addressed. Bring that exact production revision into the isolated quality branch before measuring the current baseline. Keep historical captures immutable and retain all candidate fixes, including unresolved-subject clarification, source identity, source expiry, complete rule context, calendar filters and community-local dates.

Before: the experimental candidate has neither the full current reviewed source package nor all its retrieval/coverage controls. Intended after: the candidate contains both the current production source boundary and earlier shared quality fixes. This integration alone does not establish better resident answers or a winning model. Relevant families include form/process requests, multi-part rules, report findings, current operations, ordinary hours/booking, ambiguous follow-ups and calendar filters.

## Authority and reuse

Import exact existing source decisions, versions, quoted claims and actions without expanding approvals or altering withheld claims. Adopted rules remain authoritative for binding requirements; reviewed process records and forms support only their own allowed steps/actions; live connectors retain their current operational scope and failure behavior. Community-specific facts stay in the existing profiles and source records. The merge changes no source approval, model setting, subscription or deployment.

Only three textual overlaps required resolution: union of contract/conversation imports in community-assistant, union of interpretation helper exports, and union of search helper exports. Both branches' implementations remain present. Automatically merged selection and expiry behavior must still pass the combined tests. The earlier source lifecycle gate and the newer canonical future-expiry gate are complementary; neither is removed simply because both check freshness.

The 147-file production delta includes previously released private owner UI changes; these are imported as existing production behavior, not new design proposals. No additional resident-facing UI change is made here. Preserve the dirty original workspace and untracked experimental capture/cache directories.

## Verification plan and evidence

1. Revalidate eligible approved sources into a separate temporary snapshot using the existing release bridge, without editing the checked-in index or broadening approvals. Initial result: passed, 39 due URLs checked, approved fingerprint 94c0b7d9ab0b3779fbe4009e01f9639b243ae95688e51da04a8077f2ef041bca. Exact attestation and per-source checks are in artifacts/quality-eval/current-production-integration-20260915.
2. Commit the resolved local candidate, then run the existing full release-check wrapper using that explicit snapshot. Capture exact revision, unchanged-runtime check, all test counts and any failures. No model credentials are supplied; model generation is disabled for the local release gate.
3. Diagnose failures by question family and distinguish a merge regression from a stale assertion or fixture. Do not weaken authority/completeness expectations to produce a green result. Verify earlier quality fixes and newly imported source-boundary behavior together.
4. After compatibility, refresh the matched baseline and current source scope before further model comparisons. Paid model comparisons remain blocked by the observed credit rejection. Human calibration, final unseen tests, complete cost comparison and live candidate rehearsal remain outstanding.

No staging/production test questions are submitted by this integration. Any later hosted question must include isTest true; browser testing requires the test-mode URL and visible banner. Production health at 2026-09-15 01:28:11 UTC identifies only the deployed revision/configuration, not current provider credit or answer quality.

Documentation impact: local integration and evaluation-baseline decision. Exact Notion update remains pending the earlier external-write rejection; do not relabel the candidate live or overwrite the other task's source-review documentation.

## Offline findings on the merged candidate

Local merged revision: `fc758f584c7679be99f224e4c7e3fee23a906395`. The snapshot validator independently confirms that the separate revalidated index matches the merged checked-in source evidence and approval scope. Revalidation began before the merge commit, so its attestation records the preceding HEAD; the input and approval fingerprints, rather than that incidental pre-commit label, bind its exact data. The full check uses the committed merged runtime and this snapshot.

The current eligible corpus contains **88 approved community projections, 26,794 text characters and 92 action entries**. The general architectural form (1574) and landscape packet (1964) are present with scoped approved text and actions. The earlier complete-catalog experiment was deliberately limited to 50 projections / 10,000 characters, and now stops at its constructor with `Complete approved catalog exceeds experimental bound`. Do not silently enlarge that limit or drop documents to make the old approach appear compatible. The next comparison should select relevant material while retaining complete approved content and authority for each selected source. A whole-library prompt is no longer the small-catalog option previously measured.

Saved-plan keyword replay covers five authored document questions. Its corrected capture uses the same `loadRulesIndex` path as the actual comparison runner, including supplements and inline topics. Earlier `current-keyword-packets.json` mistakenly read the raw rules file; it is retained only as a superseded diagnostic and must not be used to assess runtime rules retrieval. Use **current-keyword-loaded-packets.json** instead.

The corrected keyword replay finds the governing lighting policy and shed section, but still misses the approved project forms. Lighting retrieves landscaper/clubhouse/water-report/billing actions; shed retrieval includes a water report; the two pergola plans lack the specific pergola section. A generic application question finds the official directory but also an advisory-committee page. These are experimental retrieval packets, not production answers or representative quality scores. Source availability alone does not fix need-to-source relevance. Adding a vector database has not been proved necessary: bounded local semantic ranking and existing approved action context should be compared before a service commitment.

A separate five-question check used the **merged local coordinator with models disabled and all network blocked**. No network attempt occurred. It is an offline diagnostic, not production's configured AI behavior or a cost baseline:

| Requested task | Observed local response | Diagnostic issue |
| --- | --- | --- |
| Find the design review application | Points to contact/submission page; complete | Does not use the available application destination. |
| Permanent-lighting approval process | Names permitted systems, termination elevations and shutoff time; complete | Omits the application/submission process and required cut sheets. |
| Shed height and application form | Basic limit plus utilities/screening and general links; complete | Omits the qualified height exception and requested form identification; adds an unrequested placement gap. |
| Hot tub plus pergola permission | Says a lighting passage cannot prove pergola permission; missing evidence | Fails to retain the independently answerable hot-tub part and does not retrieve the pergola authority. |
| Pergola permission and form | Gives lighting requirements and labels complete | Wrong-subject rule answer, despite having a useful form link. |

Evidence is in **model-disabled-current-answers.json**, including full returned sources, actions and completion labels. These small known-case observations do not establish prevalence or an owner-calibrated score. They do show why software-test success must remain separate from resident answer quality. The next shared retrieval/answer-completion change must handle primary subject, required facets and independently answerable compound parts; do not introduce pergola-specific answer wording or merely penalize a label after an unrelated answer has been written.

The actual private-log entry builder was also run locally on those five saved responses, with `isTest: true` and **no log submission**. It rated the generic application handoff Weak / High resident effort. It rated the other four responses Good / Resolved, including the wrong-subject pergola answer and the compound answer labeled missing evidence. This directly reproduces the owner's rating concern on the merged candidate; it is not an estimate of traffic-wide false-positive frequency. Capture: **offline-rating-audit.json**. Merely requiring a nonempty answer, sources, a form link or complete status cannot establish resolution, and an honest partial answer must not be downgraded indiscriminately.

## Completed compatibility check

The full existing `npm run check` chain, including precheck and postcheck, **passed** on runtime revision `fc758f584c7679be99f224e4c7e3fee23a906395`. Process 71287 exited 0; the independent wrapper result records `candidateUnchanged: true`, zero failures, and completion at **2026-09-15 01:48:11 UTC**. Evidence: `full-check.json` and `full-check.log` in the integration capture. No running process remains from this turn.

- 1,219 unit tests passed; zero failed.
- Existing rules evaluation passed 140/140 variants; existing seven-case unseen rules suite passed 7/7. This small pre-existing suite is not the goal's new frozen unseen acceptance benchmark.
- Resident regression corpus processed 122 questions, with two explicitly withheld/unscored.
- Community audit processed 249 unique questions, with five withheld/unscored and no regression according to its existing scoring rules.
- Controlling-source retrieval check passed 20/20.

These are compatibility checks, not independently calibrated useful/excellent rates. The separate five-question audit above still exposes wrong-topic and missing-detail responses and false-positive Good/Resolved ratings. The regression gate's ratings cannot override those observed defects. Source refresh and this local integration do not prove live candidate readiness. No deployment, source approval, subscription or paid model call occurred in this integration turn. A generated untracked `data/community-retrieval-report.json` is release-check output, not a modified source library.
