# Controlled writer comparison — September 14, 2026

Planned diagnostic only; no resident behavior, model selection, approval or release change.

## Preimplementation record

The latest semantic full-flow comparison supplied the right evidence yet produced two shared failures: converting a qualified requirement into an absolute rule, and claiming a project-specific form exists from an official directory alone. These affect rules and task completion across communities. Before adding more instructions or a checker, isolate whether another eligible writer handles identical input better.

Compare Haiku 4.5, Sonnet 5 and Opus 5 on the saved shed and lighting-follow-up composition requests from the first Haiku trial for each case. Repeat twice per model, randomized across 12 calls. Each model receives exactly the same source packet, original question, prior resident context, interpretation, actions, system prompt and output schema. Only the model ID and supported temperature option change. Keep thinking disabled and the same 650-token output bound. The current provider documentation permits Opus 5 thinking-disabled requests at the default high effort; no effort override is introduced. Fable and Astra are excluded.

The source snapshots must still validate at execution time. Reuse exact captured packets; never promote a staged form, fetch missing private content or modify a source. Fail on source mismatch or API failure; preserve shape failures as failures, not silently repaired answers. Save anonymous answer-review material, raw structured outputs, tokens, latency and exact request hashes. This is a writer-only comparison, excluding fresh interpretation, retrieval and hosting costs; it cannot establish full-answer latency, a representative success rate or human calibration.

The entire balanced design reserves $0.885436 before any call, within the $1.180031 remaining from the semantic full-flow phase's $3 ceiling. The executable preflight recomputes the exact reservation and refuses overflow. No subscription or paid embedding. Existing rate table checked against [provider pricing](https://platform.claude.com/docs/en/about-claude/pricing); Opus 5 is $5 input/$25 output per million standard tokens. See [Opus 5 behavior documentation](https://platform.claude.com/docs/en/models/opus-5/whats-new-opus-5) for thinking controls and output-shape limitations. These sources were fetched September 14.

Reusable boundary: evaluation harness only, no resident facts in core application code. Checks must establish paired request identity, restricted models, packet binding, no silent prompt alteration, and budget arithmetic. An improved answer on two known cases permits further evaluation, not production adoption. Live-adapter integration, unseen testing and independent human review remain outstanding.

## Completed result

Finished 22:34:28 UTC at experimental revision `6c5e648`. All 12 calls completed, with valid draft shape and known usage. Zero repairs, no extra interpretation calls, no new embeddings. Model identities returned by the provider were Haiku 4.5's dated alias, Sonnet 5 and Opus 5 as requested. Two focused harness tests pass, including paired request preservation and captured evidence/action consistency; a no-call preflight verified all 12 real captured inputs before execution.

Primary-agent development review found:

- **Haiku:** both shed drafts again removed the qualified height language and explicit exception. Both lighting follow-ups acknowledge that the exact form is unverified, but introduce additional contact routes and uncertainty about the approval process instead of keeping the next step focused.
- **Sonnet:** both shed drafts preserve the height qualification and exception. Both lighting follow-ups assert a general application applies or describe what the directory lists without evidence establishing the exact form or directory contents. A caveat at the end does not undo an unsupported claim at the start.
- **Opus:** both shed drafts preserve the qualification and exception, and both lighting follow-ups explicitly say a specifically named lighting form cannot be confirmed. These responses are more restrained than Sonnet's form assertions. The shed drafts remain lengthy (183 whitespace-counted words each versus the requested 180), and both form answers add submission instructions beyond the immediate location question. No complete factual audit or excellent rating is claimed.

This supports further Opus evaluation for writing, not a production model choice. The review is not independent human calibration or a blind assessment: the primary agent saw model identities in run progress. The anonymous review file remains available for a later reviewer. Four runs per model over two known cases cannot establish statistical significance, a representative quality rate, or the best stage-by-stage allocation.

## Measured composition cost and time

Each model made four calls on identical paired content; different tokenizers yield different input-token counts. Thinking was explicitly disabled. No prompt-cache tokens were reported. Standard provider token prices were rechecked September 14; no fast mode or subscription was used.

| Writing stage only | Haiku 4.5 | Sonnet 5 | Opus 5 |
| --- | ---: | ---: | ---: |
| Input / output tokens across four calls | 29,108 / 909 | 38,898 / 1,648 | 38,626 / 1,433 |
| Total test cost | $0.033653 | $0.094276 | $0.228955 |
| Cost per composition | $0.008413 | $0.023569 | $0.057239 |
| Cost per 1,000 compositions | **$8.41** | **$23.57** | **$57.24** |
| At 10,000 compositions/month | $84.13 | $235.69 | $572.39 |
| At 50,000 compositions/month | $420.66 | $1,178.45 | $2,861.94 |
| Median composition time | 3.80 s | 6.01 s | 5.98 s |
| Observed range | 2.60–4.13 s | 5.09–6.47 s | 5.32–6.61 s |

These monthly scenarios repeat this two-question mix and one writing call per question. They exclude interpretation, retrieval, acceptance, repairs, indexing, storage, hosting and source maintenance. They must not be compared directly with full-answer costs or quoted as a total implementation price. An Opus full-flow measurement is still missing; adding a mean interpretation price to this different case mix would not supply that measurement. See the prior [complete semantic-flow comparison](COMMUNITY-SEMANTIC-FULL-FLOW-COMPARISON.md) for current-local, Haiku and Sonnet full answering figures and their limits.

This replay spent **$0.356884**, with all usage known. Combined with the preceding semantic full-flow run, the phase spent **$1.014419**, reserving **$2.705405 of $3**, with **$0.294595 reservation capacity remaining**. Earlier phases remain separately accounted. These are token-priced estimates, not reconciled provider invoices.

Evidence: `artifacts/quality-eval/paired-writers-20260914/manifest.json`, `paired-inputs.json`, `calls.jsonl`, `comparison.json`, individual `writer-*.json` outputs and `blind-review.jsonl`. No resident records were supplied. No runtime change or release occurred. The next useful comparison needs a broader set of faithful-evidence tasks and a complete Opus answer flow, while preserving the ability to use cheaper models in other stages. Owner quality calibration and live-adapter coverage remain required.

Documentation impact: decision/audit checkpoint only, no resident-flow diagram change. Notion remains pending under the earlier automatic approval rejection; the exact update is saved in `docs/pending-notion/2026-09-14-writer-replay.md`.
