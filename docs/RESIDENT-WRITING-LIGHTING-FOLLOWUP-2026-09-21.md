# Final writer integration follow-up

Production verification of PR #177 found an adjacent selection problem after the reported Halloween decorations answer passed. The question about leaving lighting on all night retained seasonal dates and removal instructions but omitted the verified lights-off time. The cutoff remained present in the underlying rule answer: the focused permission selector ranked repeated topic words above the requested overnight condition. Plain imperative wording also lacked the modality keywords used by downstream permission checks, and the duration checker did not recognize a clock cutoff. The writer correctly cannot restore facts absent from its selected claim set.

Repair the shared overnight-permission family: prioritize verified clock/overnight conditions during selection, recognize explicit source-backed turn-off instructions as obligations, and prevent unrelated seasonal dates from completing an overnight permission request. Clock cutoffs satisfy a timing request. Validation also normalizes sentence-leading prepositions before proper names, without changing displayed wording or accepting new names. No holiday answer, cutoff value, source approval, community URL, or UI design is added.

Before/after: a resident asking about overnight use receives the relevant verified cutoff instead of only installation dates. The writer must keep that cutoff or fall back. Current controlling evidence, source identities, review/freshness, and failure behavior remain under the existing contracts. Tests will include actual audited lighting questions, alternate-source clock values and subjects, missing cutoff rejection, changed-value/name rejection, and unrelated display questions. Run focused checks, the complete applicable release gate and required protected checks; verify exact staging and production using marked test questions. Final release links and documentation readback belong in the follow-up PR and owner guide.

The real-model staging check then exposed inconsistent permission-language checks: the rules and completion checks accepted “You need to turn off,” but the separate model-composition goal checker rejected it. Those three checks now share one language recognizer. A test exercises the actual composition validator with ordinary imperative and “No, you need to” wording and unchanged source-provided clock values, so a stubbed writer cannot hide this integration failure.
