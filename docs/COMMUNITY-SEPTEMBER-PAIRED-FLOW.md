# Current-source paired full-answer diagnostic

Predeclared September 15 UTC. Compare current-local legacy/selective Haiku with an experimental compact Sonnet 5 interpreter and Sonnet 5 writer, local semantic community search plus hybrid rule search, reused keyword preparation and the shared need/evidence capability inventory. No inline model checker or repair: both tested checkers were rejected. No model, recurring spending or release is selected by this comparison.

Twelve known development questions, twice per arm, produce 48 intended full-answer trials: recycling/storage, today's truck/menu, pool status/hours, yoga follow-up, shed rules/form, lighting follow-up, water payment, CAB report, nanny access, Great Hall booking, coffee password and unanchored price. Preserve original wording and resident-only follow-up context. Freeze randomized trial order before any model call. Include missing answers, invalid interpretations, source failures and incorrect responses in all denominators. Anonymous review records omit model and automatic score; creating these records does not establish completed blind human review.

Current community snapshot: current-production-integration-20260915/community-index.json and its attestation, independently validated against the current checked-in index. Current loaded rules exactly match the earlier verified rules snapshot hash 23dfe59761384008e7f119185e2895552c67fe028e7b30f314a2c228355db098, with attestation valid until September 16 20:27:43 UTC. Check identity, source scope and expiry again before each trial; do not refresh dates manually. Capture current public live results per trial. Different observations or source availability must be considered before attributing an answer difference to a model.

Bound model requests to a $5 conservative reservation ceiling from existing API credit. This is a separate current-source full-flow diagnostic, not a reset/retry of the closed checker or prior mixed-flow experiments. Record all calls and unknown charges; stop on provider errors or reservation exhaustion. If the design cannot finish within the ceiling, report the incomplete paired comparison without ranking uneven arms. No purchase, subscription, deployment, settings change, resident-log question or new source approval. Direct local test outputs carry isTest true. Existing test labeling rules apply to any app requests.

Primary review: actual question resolved, all requested parts preserved, facts and qualifications supported by exact eligible evidence, relevant usable actions, accurate remaining gaps, and readable specific prose. A present draft, matching source ID or model completion flag is insufficient. Inspect known semantic failure families including assumed lighting approval/operation, missing menu need, exact roof-pitch qualifications, form applicability, caregiver versus guest membership and unrelated password fallbacks. These known cases cannot substitute for the final unseen benchmark or owner calibration.

Measure full local latency, stage token costs, failed attempts and local initialization/resources separately. Current-local is the integrated local baseline, not a claim about exact deployed billing. The historical planner and writer stage costs must not be added and called this new flow's measured price. Hosting/embedding API charges and actual deployed resource changes remain separate.

Implementation verification: 20 focused tests pass, including compact full-flow propagation, both semantic index lifecycle/tenant boundaries, request/source identity, and explicit unreviewed output handling. Syntax checks pass. Default experiment behavior remains unchanged without the September comparison flag. This is a local experiment only; owner guide synchronization remains pending the prior automatic external-write approval rejection.
