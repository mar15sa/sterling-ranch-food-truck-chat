# End-to-end candidate comparison

Planned September 14, 2026 before implementation. Local experiment, not a production model decision. Previous checkpoint produced completed grading/acceptance evidence and commits; it was progress, not a wait or blocked turn.

The coordinator can replace the requested subject/outcome with a nearby retrieved answer. Better prompts in one stage cannot fix that if later stages discard the need. Build one isolated candidate that interprets the original resident wording and resident-only context, assigns stable need IDs, retrieves eligible evidence for those needs, composes an answer, and applies independent compact acceptance to the same needs. Every composed attempt, including one optional repair, must pass the same source/action/coverage boundary. After a failed or unavailable check, never return the unchecked draft. Report an unresolved local result and preserve the failure for evaluation.

Before: an unanchored cost question may get water rates, and a requested form may get only submission instructions. Proposed after: clarify without guessed retrieval for an unanchored subject; retain each requested object/action and disclose an unavailable exact form. Broad clear questions must still get supported answers. Honest partial answers remain partial and useful rather than falsely complete.

Use existing rules lifecycle/replacement/owner-review eligibility and same-version section context, plus the community search's exact approved claim/action projections. Do not pass raw static community pages or their unapproved facts to a model. Bind the rules snapshot explicitly to a community and verify its configured source URL; never infer another community from a shared hosting domain. Preserve source versions. Actions are selected by existing reviewed IDs/destinations, never generated URLs. A rule may support governing requirements; an action projection cannot prove a fee or permission. No new source approvals.

First compare document-based questions, including the known-good recurring trash answer and authored ambiguity/follow-ups/compound requests. Current control uses the repository coordinator with observed current production model settings; it is not an exact measurement of the deployed revision or live billing. Both arms use the same local snapshots, date and community, and record code/data hashes. This scope does not cover live events/status/schedule adapters, and cannot establish full-system readiness. Live-adapter integration, semantic-vs-keyword comparison in this flow, frozen holdout and human review remain mandatory subsequent work.

Stage candidates: refined Haiku interpretation; Haiku versus Sonnet composition; Sonnet compact acceptance initially, with Haiku acceptance retained for later comparison. Exclude Fable/Astra. Do not select based on this pilot. Capture all input/output tokens, every failed attempt, one bounded repair and all request durations. Stop on provider errors or a $5 conservative reservation cap. No subscription, release or changes to provider settings. Detailed log grading is not included in these answering costs and must be added if later selected.

Require offline checks for two tenant profiles, mismatched source versions, unknown source/action IDs, missing/duplicate needs, unsupported source roles, malformed interpretation, composer failure, checker failure and failed repair. No resident-facing UI change. Experimental modules are not imported by resident routes. The Notion checkpoint and proposed flow diagram will remain prepared locally while the earlier external-update confirmation is pending.

## Development findings before the revised capture

One initial strict-planner request timed out at 20 seconds with unknown usage. The next attempt stopped on a local harness date-type error; the control now receives a Date as expected, verified offline. The completed strict-planner run captured 48 trials, with 28 of 32 candidate trials rejected before composition: escaped tool markup appeared inside the clarification string and the needs array was empty. Restore the previously tested non-strict Haiku interpretation request and retain local validation. Strict composition/checking is a separate choice.

The following run exposed a contract integration error: asking the checker's existing prose `request` field to carry a need ID was unreliable. Give that identity its own required `needId` field instead; keep `request` descriptive. Do not repair an answer merely to work around the checker's mismatched schema. The same run captured two timeout attempts in the current rules-rewrite path. Those are real attempted calls, not zero-cost answers.

For the next capture, expected bounded timeout outcomes remain in the sample, including the current control's existing retries. Stop on provider rejection, unexpected transport errors, six unknown-usage attempts, or the conservative reservation cap. This replaces the earlier stop-on-any-timeout policy, which prevented comparing ordinary failure behavior. No automatic retry of a provider rejection is permitted. The combined reservation across these development captures remains at most $5; each new run receives only the unused remainder. A longer 45-second diagnostic request deadline measures slow calls honestly and is not an adopted resident latency setting.

Per-need community search now consumes the interpreted task/facet instead of reclassifying its query words: a form associated with an approval stays a form search. Rule documents are also checked against the bound community path, not just a common official host. Exact experimental code is saved for v3 and subsequent captures and hashes verified, so those uncommitted prototype revisions remain reproducible. Earlier failed harness attempts retain hashes and request evidence, not a complete source copy.

## Stop condition: invalid freshness eligibility

The v5 run was explicitly stopped after 25 completed question records. A read-only audit verified the exact repository snapshot hash against the capture and found all ten supplied static community records examined had expired before the run. These included recurring waste guidance, the application directory and unrelated action links. Their stored `lifecycle: current` label had not changed when `staleAfter` passed. Search's canonical projection path checked approval identity but skipped the parent source's expiry. This is a demonstrated shared runtime defect; repair and regression evidence are in `docs/COMMUNITY-EXPIRED-PROJECTION-REPAIR.md`.

The output facts are not thereby proven false, but neither arm of this local capture establishes current-source-safe quality. Do not label model-accepted outputs as human-rated usefulness. Some drafts also used source IDs as action IDs, repeated a failed draft during repair, exceeded the requested writing length, or pulled historical process contacts from a governing section. These remain candidate contract/composition problems, not reasons to release the candidate. A checker rating an answer complete is insufficient proof of human-first resolution.

Recorded end-to-end development attempts through v5: 123 journaled provider calls, $1.151095 known token-priced subtotal. Five timed-out calls have unknown charges, plus a possible additional request in flight when the process was stopped. This is not an exact total bill. The conservative reservation allocation across the five runs was at most $4.996239 under the $5 boundary; the final run's incomplete manifest reservation alone is not the full interruption bound. No new subscription or provider setting change occurred.

The completed strict-planner v3 control answered all sixteen local control trials with $0.016377 in captured model charges (about $1.02 per 1,000 at that narrow test mix). Its candidate arms mostly failed interpretation and therefore cannot supply proposed-system cost estimates. The later incomplete v5 run's richer candidate prompts and repairs cost materially more, but its unbalanced cases, invalid source eligibility and unknown charges preclude a defensible current-versus-proposed implementation commitment. The full comparative cost requirement remains open.

Next: finish the shared expiry repair's full tests, prepare an actually current exact-approved local source snapshot through the existing refresh workflow, and resolve claim-role/action identity issues before another bounded paid comparison. Retain the requested full topic context while separating current process/contact claims from governing restrictions. Do not solve missing approved forms by approving them implicitly. Live adapters, semantic retrieval in this flow, human calibration, independent blind review and the frozen holdout remain outstanding.

## Preparation after the expiry repair

The shared runtime repair passed 860/860 checks and is committed locally as `0e628d5`; it is not released. Official revalidation of a separate community snapshot then passed all 60 URL groups with the existing approval fingerprint. Rules snapshot provenance/freshness remains a separate requirement; this community refresh alone does not verify the Municode snapshot.

Before another paid run, require the passing community revalidation attestation, verify its input and approval identities against the repository, recheck freshness at run time, and save the actual snapshot with the experiment. Reject failed, altered, or expired input before making any model call. Historical captures keep their original snapshot checks. Make composer action IDs an explicit schema selection from the packet action inventory; evidence IDs are not action IDs. The resident runtime and approvals are unchanged by these experiment preparations. Check these boundaries offline; no further paid run is authorized by this paragraph, and the prior combined development reservation remains exhausted.

Offline preparation checks passed 8/8, including unchanged renewal, failed verification, expiry, future-dated verification, altered approval/text/version, independent source-audit failure, cross-community binding, and action inventory selection. The actual 60-group snapshot passes the new loader. The full runtime suite result remains 860/860; the subsequent experiment-only changes do not imply a new paid comparison or model-quality improvement.

## Verified rules snapshot and partial-need contract

September 14 official verification: the latest Municode publication is still job 435037 and its version metadata matches the saved base documents. All twelve named supplement PDFs match their recorded text versions. For four restricted supplements, a separate verification-only extraction matches the full-text hashes in historical revision `fdc981e`; the existing owner-approved projections remain unchanged and are the only text eligible for answers. None of the verification-only full text is loaded into the candidate. This does not prove no undiscovered amendment exists. Exact evidence is under `artifacts/quality-eval/current-rules-snapshot-20260914` and `current-supplements-snapshot-20260914`; production and repository source data are unchanged.

The previous flow also exposed a schema-level cause of unnecessary repairs: a single requested process could be partly answered, but the compact checker only had addressed, disclosed-gap, unanswered, and clarification-needed. It sometimes returned overall partial with the need addressed, which local validation rejected and sent to composition again. Add an explicit partial need status with supporting evidence and require overall partial. Preserve failure flags for omitted parts and unsupported claims. This is a shared outcome contract, not a lighting-specific exception. Offline checks must cover partial support, missing references, contradictory complete status, and source-role constraints.

Improve the experimental composer/checker instructions for applicability across families: supplied text is not proof that every stage, fee or prerequisite applies to the resident's project/role. Check every added claim and action, including optional extras. Preserve source approval scope and effective dates in the evidence packet. This prompting change is a hypothesis to test, not proof of quality. A process answer should prioritize practical supported steps; an exact-form gap remains a gap. No fixed resident facts or new approval are introduced.

A fresh full-flow comparison may use a separate maximum $5 reservation with the original eight diagnostic cases, two repetitions and three arms, after both community and rules attestations pass. This is a new bounded experiment under the owner's requested comparison, not an extension that hides the previous development costs. Record the previous $1.151095 known subtotal and unknown requests separately. No new service subscription or runtime selection is authorized or implied. Excluded models remain excluded.


Fresh run v6 stopped after its first case because the composer returned HTTP 400. One interpretation call cost $0.002938; one rejected composition has unknown usage. Reserved upper estimate $0.037981. The saved request included `maxItems: 0` when no approved actions existed. That constraint is unsupported by Anthropic strict schemas (official structured-output documentation checked September 14: https://platform.claude.com/docs/en/build-with-claude/structured-outputs). The observer had omitted the provider error message, so attributing this historical 400 specifically to maxItems is an inference. Remove that unsupported schema constraint and keep the existing local zero-action validation; retain enum constraints when approved actions exist. Subsequent errors now retain provider type/message. A new run receives at most $4.96, keeping this fresh comparison phase below the separate $5 limit. Do not erase the failed attempt or report unknown usage as zero.


Next independent preparation while v7 runs: integrate the already measured local semantic vectors into the same eligible per-need evidence packet, without changing the running comparison. The keyword-only flow still loses some requested-topic evidence and brings unrelated process links. Keep exact source/version/community identity, per-question owner scope, and lifecycle filtering at retrieval time; similarity cannot approve evidence. Reuse only the byte-compatible cached corpus and model revision. No paid embedding API or new subscription. Run an offline packet comparison before a later answer comparison; do not declare a retrieval winner from similarity scores. This experiment is scoped to rules retrieval; approved community action retrieval remains a separate gap.


## Completed fresh comparison and next decision

v7 finished all 48 trials with 106 provider attempts and no unknown usage: $1.554620 token-priced total, $4.251689 conservative reservation. Combined with v6, the fresh phase has $1.557558 known cost and one rejected unknown-charge attempt; the reserved upper total is $4.289670. No process remains running. Per 1,000 attempted questions at this exact small mix: local baseline $1.008875, Haiku writer with Haiku interpretation/Sonnet acceptance $39.159688, Sonnet writer with Haiku interpretation/Sonnet acceptance $56.995188. Both candidates produced an accepted answer on 14/16 attempts. This is not independent usefulness: at least one accepted lighting-application follow-up offers an irrelevant landscaper-directory action. p95 times are 4.915s baseline, 22.915s Haiku-writer and 34.698s Sonnet-writer. Neither candidate is a release recommendation. Full stage tokens, monthly scenarios, exclusions and findings are in `docs/COMMUNITY-FULL-FLOW-COST-DECISION.md`.

The post-capture audit verified the exact saved source snapshot and found zero expired static community records among ten supplied to composers. The v7 rules attestation covers all twelve named supplement documents and the main publication metadata. Both attestations retain their verification limits.

The separate semantic packet replay completed seven captured interpretations with existing local vectors and no paid API. It recovers the specific gazebo/pergola section missing from the compound and multiple-form keyword packets; lighting's unrelated community links remain unchanged. Local hybrid packet times were 645–2,194ms versus keyword 468–1,500ms across these seven cases, including repeated queries for separate needs. This is retrieval evidence, not final-answer quality, hosting cost, or an unseen test. The semantic preparation ran after v7's paid comparison ended, so local embedding computation did not compete with its latency measurement. Eleven focused flow/snapshot tests pass, including alternate-ranker identity and community rejection. No resident runtime change after the 860-test expiry repair is implied.


## Approved process/action retrieval and staging form navigation

Before implementation, September 14: keyword community retrieval selects unrelated service links for approval/application questions, while the current semantic experiment only ranks rules. Build a semantic corpus from the existing exact approved claim/action projections, using the shared source-review gate and projection function. Raw page/PDF bodies must never enter this corpus. Keep current lifecycle/expiry, URL/version/community, fact scope, and dynamic-connector exclusions. The same generic ranker should accept this approved projection corpus; vectors are retrieval aids and cannot create evidence. Compare keyword and semantic community ranking on multiple process/action families, plus changed-version and cross-community negative checks. Do not change production retrieval or review decisions merely to run this experiment.

A source audit found existing owner authorization that earlier experiments had not used: `data/community-form-approval-evidence.json` records Marissa's September 6 approval of exact 2026 architectural and landscape form versions for staging only, also represented in the canonical ledger. Production eligibility still correctly requires scoped claims; the staging-only approval is not production approval. A separate, explicit experiment option may project only the original PDF destination/title from those exact currently revalidated staged documents. It must verify every approved chunk/version and document identity against the saved approval and current index, restrict to the same community and official host, add no PDF facts, and carry `stagingOnly: true`. It must never modify the canonical production ledger or infer a required form for a project. This narrower staging experiment is covered by the existing authorization; don't ask the owner to approve the same testing scope again. Production form-link approval remains a separate decision before release.

The specific before/after test: a lighting application query currently retrieves a landscaper directory or facility links; a meaning-based search across approved action/process projections should retrieve the design-review directory/contact. The explicit staged-form variant may also return the approved document destinations, without claiming project-specific applicability unsupported by evidence. Adjacent payment, booking and contractor/action topics must remain distinct. Expired, changed, cross-community or unapproved records must be absent even if semantically similar. No new subscription or paid embedding API.


Corpus inspection finds only 25 production-eligible static projections totaling 5,502 text characters. Test the complete approved process/action catalog first, alongside scoped rule retrieval, before adding an embedding or database layer for this small catalog. Two separately flagged staging-only form destinations can be added under the recorded authorization. This is an explicit experimental mode, default off; it does not broaden the production source ledger. Bound the catalog and report overflow instead of silently omitting part of it.

Another shared cost/attention defect was demonstrated in v7: two lighting hits each expanded to the same 11,156-character, same-version policy, but the packet deduplicated by the selected chunk ID and sent both copies. Deduplicate expanded context by its full exact chunk/version identity, retaining all matched source IDs and all evidence once. Do not merge different versions, communities, scopes or differing action sets. Regression evidence must show one copy of a shared expanded section and preserved provenance. This reduces repeated input without shortening the approved policy.


For a need explicitly classified as an official action (form/payment/booking/etc.), the complete-catalog experiment does not fetch binding-rule text unless the plan contains a separate governing or process need. Locating a requested document is not the same as asserting that the resident must submit it. This avoids inventing fees or prerequisites from unrelated rules. Mixed permission-and-form questions retain separate evidence obligations. The final checker still judges the original question and may reject an incomplete interpretation.

The model receives all selected approved text, titles, roles, source URLs and scope restrictions, while exact hashes, chunk lineage and repeated internal metadata remain checked and recorded by software. Removing internal metadata is not removing evidence. Each selected action will receive its own explicit relevance review tied to the requested need, with a reason. Each hard failure must include an actionable explanation for one bounded repair. Approved-but-irrelevant actions must trigger failure. Test useful fallback contacts separately so missing exact forms do not become a blanket rejection of helpful partial answers.
## September 14 action-check and catalog checkpoint

Locally implemented evaluation tools, not selected or deployed. The existing runtime projection function is exported for reuse; resident search behavior is unchanged. Complete-catalog retrieval and staging navigation are off by default. Existing September 6 owner approval covers testing the two exact form documents in staging only, without production promotion or claims about fees, requirements, or applicability.

The acceptance pilot completed ten calls: 8/10 valid expected results, with the known wrong lighting action caught twice and all six useful controls retained. Two mixed correct-form/wrong-extra examples produced inconsistent failure codes and empty explanations, and were rejected by local validation. Median checker latency 10.738 seconds and p95 18.720 seconds are unsuitable for the proposed full-answer target. Cost was $0.134170; the full cost decision records limitations and cumulative reservations. This is diagnostic evidence, not independent quality validation.

Seventeen focused tests pass across action review, snapshot gates, two-community isolation, approved projections, staging-only exact-version navigation, bounded repair, full-section deduplication, and retaining separate governing evidence for permission needs. Offline catalog replay completed seven saved interpretations in three modes without provider calls. It saves each packet and prompt-projection byte counts. Those bytes are not token-priced savings or final-answer quality. Catalog expansion improves form destination availability, but increases packet size for several other questions. See `complete-catalog-replay-v2-20260914/report.json`; earlier preparation counts predate the form-only retrieval change.
## Planned runtime repair: clarification lost to broad intent classification

Root cause: the coordinator defers an AI clarification whenever the broad input classifier returns rules-question. That classifier deliberately recognizes request shapes such as “how much” and “can I”; it does not prove an identified subject. An unresolved short reference can consequently reach rules retrieval and return a confident wrong-topic answer.

Affected family: context-dependent cost, permission, timing and location questions with an unresolved reference. Before: a correctly requested clarification can be displaced by unrelated retrieved facts. After: reuse the conversation resolver's existing dependency decision; a still-dependent question with a validated clarification stops before retrieval. Resolved resident context and complete named-topic questions continue through normal authoritative retrieval, including the existing tests where an unnecessary AI clarification must not block an answer.

Reuse boundary: extract the existing dependency decision without changing its semantics, rather than add a topic list or supply answer facts. Apply it to the coordinator's clarification deferral. No source approval, live connector, profile, model, UI layout or release changes. Test two communities and multiple requested facets, preserve context resolution and complete-question behavior, then run the full runtime suite and resident-literal guard. Model calls are not needed for this deterministic regression. Live connector integration into the experimental full-flow candidate remains separate and incomplete.
## Planned shared live-calendar repair: range aggregation loses a failed day

During live-adapter preparation, an offline two-community reproduction showed that a two-day calendar request changed a daily degraded result into a healthy range result. One day's refresh failed and retained its old calendar; the other refreshed successfully. The range combiner unconditionally emitted healthy/ok, complete date coverage, and the newest day's checked time. A separate request for the failed day correctly remained degraded.

Affected family: multi-day event, activity, and calendar requests during partial refresh failures, independent of wording or community. Before: the combined calendar could appear fully checked despite a failed constituent day. After: propagate any daily degradation to range-level coverage/outcome and retain the oldest constituent observation time and earliest expiry. Keep existing useful retained event data and safe resident failure handling; do not invent an empty schedule. An expired daily cache still causes the existing whole-request failure. Reuse the existing calendar envelope contract, without new profile facts, model calls, approval, or UI changes.

Tests must cover two profile-configured communities, one/all failed daily refreshes, fully healthy ranges, retained expiry, and the existing single-day behavior. Evidence before: `artifacts/quality-eval/calendar-range-degradation-before-20260914.json` (synthetic official-source fixtures, no external requests). Full candidate suite must be rerun after this independently discovered runtime defect is repaired. Release and live integration remain pending.
## Verified local runtime checkpoint: clarification and calendar aggregation

Both planned repairs above are implemented locally. The clarification regression failed before the edit because the unspecified cost question invoked rules retrieval. It now passes fourteen question/mode/community combinations with no rules or live retrieval when a validated clarification is still needed. Resolved resident context and named-topic negative controls remain eligible for normal answering. This does not solve cases where no valid planner clarification is produced; the broader interpretation/composition comparison remains necessary.

The calendar regression also failed before the edit: a degraded day became healthy in the combined range. It now preserves degraded/partial state, empty verified date coverage, the oldest observation, and earliest expiry, while retaining data for existing safe failure handling. Fully healthy ranges and expired-source failure behavior pass in both configured communities.

Final full runtime suite: **874/874 passed**, zero failed/cancelled/skipped, 277,323 ms. Evidence: `artifacts/quality-eval/clarification-calendar-suite-final-20260914.log` and its exit-code file (0). The earlier `clarification-dependency-suite-20260914.log` is an interrupted run with no final result; its missing process was verified before the final run was started. The final run covers both repairs. Syntax and resident-literal checks also pass (57 tracked migration-debt nodes).

No paid model calls, new subscriptions, source approvals, deployments or resident records were added. These are offline local regression results, not live verification or the independent demo benchmark. Notion's precise pending additions record the flow/diagram changes and remain unsynchronized after the earlier external-write approval rejection.
## Next bounded comparison: composition with independent review afterward

The serial acceptance stage failed to justify its latency/cost and sometimes returned contradictory repair feedback. Test interpretation plus composition without a resident-path acceptance call. Keep the same evidence eligibility, scope, version, action-ID and draft-shape checks. Mark all resulting answers unreviewed-experiment and expose no verified completion outcome; this is a measurement variant, never a runtime or release bypass. Clarifications also remain unreviewed until independent evaluation.

Compare the current local runtime against Haiku and Sonnet writers, both using Haiku interpretation, on the eight diagnostic cases twice. Candidate retrieval uses the complete currently approved community catalog and deduplicated applicable rule context. Do not enable staging-only form navigation in this comparison: this isolates the production-eligible catalog option without assuming production approval. Semantic rules ranking and live-operation integration remain separate, explicitly untested here. The control now includes the local clarification/calendar repairs.

New comparison phase ceiling: $3, announced before execution, using the existing provider account under the owner's requested model comparison. Preserve earlier phase totals/unknowns separately; do not reset their accounting. Record all calls, failed attempts, tokens, snapshots and code; validate source attestations before spending and before each trial. No new subscription or deployment. Stop on provider/schema errors or the reservation ceiling. Independent review, calibration and frozen holdout remain required before selection. Any asynchronous grading cost must be shown separately from answer latency and added to the proposed operating total when applicable.
## Simpler-flow result and retrieval-isolation follow-up

The simpler catalog comparison completed 48/48 trials at revision d88c7bb: 68 provider attempts, $0.619719 known token-priced subtotal and two current-local rewrite timeouts with unknown charges; reservation $1.808321 of the announced $3 phase ceiling. Both candidate writers produced 16/16 unreviewed drafts. Haiku answering cost $11.318688/1,000 and median/p95 9.249s/17.114s; Sonnet $26.420813/1,000 and 9.574s/11.797s. Current-local median/p95 1.402s/15.019s; its $0.015887 known cost is not the total because two attempts lack usage. No independent review/hosting charge is included. These numbers are not a complete operating commitment.

Analyst inspection found improved directory selection but material remaining failures. In flow-014, a Sonnet pergola draft applied the accessory-building 8'6 height guidance; the same verified rulebook contains a separate (b)(44) pergola section with different limits, absent from the packet. Other drafts imply a project-specific application is confirmed by the general directory or turn qualified shed guidance into strict limits. No candidate is selected.

Next isolate retrieval using the same saved interpretations and composition requests for forms-multi and compound. Prepare complete-catalog packets with keyword versus cached local semantic-plus-keyword rules ranking, then run two writers twice per packet (16 composition calls). All outputs remain unreviewed. Reserve the entire balanced design before calling the provider; use at most $1.18 of the remaining $1.191679 phase ceiling. No additional budget or subscription. This measures composition with prepared evidence, excluding interpretation/retrieval cost and latency, and cannot replace the full-answer comparison or holdout.
## Semantic composition result and output-shape repair

The fixed-interpretation replay completed 16/16 composition calls: $0.373805 token-priced, no unknown usage, $0.935140 reservation. Combined with the completed simpler flow, this phase used $0.993524 known plus two unknown timed-out control charges; reserved $2.743461 of $3. No process remains running.

All eight hybrid-evidence drafts use the specific pergola limits; the keyword packets omit that governing subsection. This is a targeted retrieval gain, not eight independently excellent answers. Form applicability remains unconfirmed, some requirements are overgeneralized, and Sonnet hybrid replay-13 contains serialized answer/action markup inside its answer string despite valid outer tool JSON. The original local draft validator accepted that string.

Before future experiments, reject tool serialization inside answer prose with a shared output-shape check. Do not strip the markup and pretend the answer was validated. This applies to any topic, does not add model calls, and leaves original captured dispositions intact. Retrospective validation must be recorded separately. Tests should cover malformed tool-like tags in answer text and ordinary resident-facing prose; the experimental route alone changes, not resident runtime.

## Planned full-flow semantic comparison

The fixed-interpretation replay demonstrates a missing-evidence mechanism, but does not test fresh interpretation, retrieval time or full answering cost. Connect the already evaluated local semantic ranker to the optional full-flow runner. Keep the current-local control unchanged, and compare Haiku interpretation with Haiku or Sonnet composition, two repetitions of the same eight document questions. Use complete approved catalog context and offline review; outputs remain unreviewed. No prompt changes in this comparison, so retrieval integration is the candidate change.

The ranker must bind to the exact eligible community corpus and cached model, preserve existing approval/scope/version/freshness gates, and never replace a failed semantic arm silently with keyword search. Initialize once before trials, record initialization time separately, include each question's actual retrieval time in full latency, and dispose the model on completion or error. Fail source validation before paid trials if a snapshot expires. Synthetic tests must establish that semantic search is actually invoked, current clock and tenant identity are retained, and setup failure releases resources. Live adapter integration remains outstanding; this document-question run cannot establish whole-product readiness.

New bounded test phase: maximum $3 conservative provider reservation, separate from the completed simpler-flow/replay phase ($2.743461 reserved of its $3). No subscriptions or paid embeddings. Capture all answering attempts, unknown usage and exact code/source identities. Initialization and local hosting costs must not be disguised as model API costs. This is local evaluation plumbing only; no resident runtime or diagram change.

Completed: 48 trials, 66 provider calls, $0.657535 known cost, zero unknown usage, $1.819969 conservative reservation. Exact local revision f126d6e; process completed successfully. Eighteen integration/source checks and the summary test pass. All eight candidate pergola trials now contain the specific governing subsection and its limits. Both writers still fail evidence-use requirements and the 10-second p95 target. See [full results and costs](COMMUNITY-SEMANTIC-FULL-FLOW-COMPARISON.md); no candidate selected or deployed.

## Planned live-evidence bridge

The experimental retriever currently records live-adapter-not-integrated and discards every live need. Thus its document results cannot establish resident usefulness for events or current status. Add an optional bridge that executes the existing profile-configured calendar and pool-status adapters for explicitly resolved connector requests. Preserve each need's identity, date range, filters, adapter identity, source host, current observation and expiry. This step connects source execution and evidence; automatic interpretation-to-connector request resolution remains separate work and is not claimed complete by manually bound test requests.

Before: live needs cannot contribute evidence to the full candidate. After: a configured and current adapter result contributes an operational source and its relevant official actions alongside separately scoped rule/process needs. Governing needs must never be satisfied by that operational source. Healthy empty calendars prove only no matches in the checked range; degraded, malformed, expired or wrong-community results produce explicit gaps and no fabricated empty calendar. Existing resident connectors and their presentation remain unchanged.

Reuse the existing parsers/envelopes; add no community facts or URLs to the bridge. Connector IDs and source domains come from the supplied profile. One resolved live request belongs to one live need. Only calendar events and the existing exact pool-status adapter are supported initially; food-truck, waste and automatic binding remain explicitly pending. Apply freshness checks again after model composition/checking so a short-lived status cannot expire during an answer and still be displayed as current. Expose observation time, expiry, timezone and requested scope to the writer.

Test both community identities, date/filter mismatch, empty versus degraded calendar, malformed/expired status, failure during mixed static/live retrieval, actions and source lineage, and expiry during composition. Exercise actual existing adapters with synthetic official-page responses and a read-only real-source check where practical. No model calls or additional paid test budget are needed for this bridge validation. No resident route, source approval or deployment changes.

## Live-evidence bridge result

Implemented locally in experimental revision `3a8be26`. The optional bridge invokes existing calendar/status adapters for explicitly bound requests, validates their envelopes and emits operational evidence with the original need association and action lineage. The writer receives explicit retrieval gaps and current observation/expiry information. Candidate output is withheld if live evidence expires during writing or checking. No automatic connector resolver has been added: the benchmark's requests were bound explicitly, and this is not proof of natural-language routing coverage.

Thirty-seven focused checks passed, including actual-parser integration for Sterling Ranch and Castle Rock calendars, profile-bound status parsing, healthy empty versus failed/degraded responses, source/filter/date identity, mixed static/live needs, operational evidence being unable to prove a governing rule, and expiry during composition. Mock model responses exercise the full candidate path and receive failure diagnostics; they do not establish model answer quality. The earlier 874-test full runtime suite remains evidence for prior runtime work; the new bridge is not imported by resident routes.

A read-only public-source check at September 14, 22:42:56 UTC used the configured September 15 calendar page and current pool page. Both returned HTTP 200 and produced valid operational sources with no retrieval diagnostics. The calendar fetch took 654 ms and the pool fetch 223 ms in this one check. These are observed fetch times, not end-to-end answer latency or p95 estimates. Calendar evidence expired at September 15 01:42:55 UTC; pool evidence expired at September 14 22:47:56 UTC. The saved capture must not be reused as current evidence after those times.

Source check: `artifacts/quality-eval/live-bridge-20260914/public-source-check.json`, containing explicit test scope, implementation revision, requests, timestamps and packets. No model calls, resident question-log writes, subscriptions or release. Automatic connector/date/filter resolution, food-truck/waste integration, full-flow paid comparisons and human-calibrated acceptance remain pending. Notion update is saved in `docs/pending-notion/2026-09-14-live-evidence-bridge.md` under the existing external-write approval rejection; no resident-flow diagram change is required for this experimental-only bridge.
