# Pending writer-presentation update

Status: implemented and verified in the local experimental candidate only; no model-quality result, deployment or live verification. Automatic approval review previously rejected an external documentation write containing internal experiment/status details; confirmation to retry has not arrived. Save this exact update. Fetch the destination before a later permitted edit, preserve unrelated content, and fetch afterward to verify.

Targets: [Decisions](https://www.notion.so/3dabf909186d8139ac52ebdbf77d8bea), evidence presentation and latency experiment; [Audit](https://www.notion.so/3dabf909186d81a2a090c2cb90183e96), local verification and remaining acceptance. No resident flow/UI change, so no resident diagram revision.

Exact proposed text:

> September 15 UTC / September 14 Denver: local experimental revision daf8921 introduces two independent writer-presentation options. The first separates assistant-side evidence limits from normalized official facts and supplies correctly labeled community-local observation times. The second uses a stable strict answer schema; exact approved action IDs remain in the message inventory and are validated in software. Initial writing, repair and semantic checking use consistent evidence context. The resident path and existing experimental control are unchanged.
>
> Forty-one focused checks passed, including actual calendar/status/food/collection parser projections, two communities, timezone/date rollover, source identity, conflicting rules, unchanged qualifications, invalid actions and expiry handling. A separate audit passed on 64 saved-request transformations: sixteen historical inputs, two writer models and four known families, each in four presentation modes. It preserved facts, source identities, actions and diagnostics. Stable mode produced one common schema instead of three per-packet schemas. These are input-construction checks, not independent resident scenarios or new answer-quality measurements.
>
> No model calls or new charges occurred. The current mixed experiment phase remains $1.822022 known cost and $4.929376 conservatively reserved of $5. No subscription, source approval or deployment. Real-model tests must still establish whether the change prevents false source attribution and local-time confusion, preserves action validity and improves first-use latency. It does not establish a fix for rule-qualification or process-completeness errors. Full-flow cost/quality, owner calibration and unseen acceptance remain outstanding.

Evidence: `docs/COMMUNITY-WRITER-PRESENTATION.md`; `artifacts/quality-eval/writer-presentation-audit-20260915/manifest.json`; `scripts/quality-eval/audit-writer-presentation.js`. Historical clocks and packets must not be labeled current or verified live. Documentation remains pending until the external update is permitted, made and read back.
