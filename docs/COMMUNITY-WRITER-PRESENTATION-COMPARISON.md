# Controlled writer-presentation comparison

Preimplementation plan, September 15 UTC / September 14 Denver. Prior turn was progress: the optional presentation repair passed 41 focused tests and 64 historical input audits, but no real-model improvement was established. The active goal remains incomplete.

Run eight known authored diagnostic cases: food schedule/menu, current pool status/regular hours, recycling pickup/storage rules, shed height/application, permanent lighting approval process, application lookup, pergola permission/form and hot-tub/pergola compound permission. Reconstruct the earliest completed Sonnet writer input for each case from the mixed or semantic full-flow captures. Use the complete retrieved packets for every case, preserving their original source clock, facts, interpretation and action inventory. Do not select evidence differently across arms or use real resident questions.

Compare three arms, each twice per case with both Haiku 4.5 and Sonnet 5 (96 calls):

1. Control: original dynamic action-ID strict schema and original source presentation.
2. Stable schema: same input/instructions except a source-independent strict action-ID string schema. Exact IDs remain in the message and are validated by software.
3. Stable schema plus separated context: additionally move adapter coverage/observation metadata out of source facts, supply the profile timezone/local date and labeled observation times, and add the corresponding provenance instructions.

Control-to-stable isolates schema presentation; stable-to-combined tests the bundled evidence-context presentation and its instructions. Neither isolates each context instruction. Model is an independent factor, with only required Haiku temperature compatibility changed. Keep thinking disabled, output budget 650, no retries, no checker or live source calls. Preserve failed outputs in the denominator. This is a writer-stage diagnostic, not the full production flow, unseen acceptance or human calibration.

Randomize the 96 jobs. Retain all requests, raw provider output, selected actions, software validation failures, token usage and elapsed times. Log schema hashes and whether each is first seen for that model within this run; do not call it a guaranteed cold request because provider grammar cache state is not observable and controls may have been used earlier. Record initial observed and later timings separately while retaining every sample in the primary timing summary.

Before calls, freeze input/case hashes and reserve the whole design. The previous mixed phase closes at $1.822022 known cost and $4.929376 conservatively reserved of $5; its $0.070624 residual is not reused. This is a separate **maximum $5 diagnostic phase**, with a computed conservative upper estimate of **$4.926744 for all 96 calls**. Do not start a partial design if preflight fails. No subscription or recurring service commitment. Actual cost must use provider token counts and retain unknown usage explicitly, not treat the reservation as a bill.

Review semantics as well as formatting: correct requested subject, all requested parts, exact rule conditions/qualifications, governing versus live authority, directory-versus-exact-form scope, no unsupported menu absence, no unlabeled UTC-local conversion, no inferred closure cause stated as observed fact, and relevant next actions. Specifically retain the shed's conditional height exception and restricted-lot qualification, lighting cut sheets/approved submission route, Ascent pickup plus storage requirement, both requested structures' approvals, and explicit exact-form/menu gaps. Do not reward longer answers for adding irrelevant rules. Compare current captured sources before judging a claim; neither a structural pass nor a selected-source match proves factual correctness.

The original sources and their eligibility remain unchanged. Existing packet and action validators apply using the original historical clock. Every replay is test-marked and must remain labeled historical. Invalid request/schema/usage failures stop further calls without erasing previous results or silently retrying. No resident integration, source approval, UI change, deployment or new subscription is authorized by this experiment. Notion decision/audit synchronization remains pending under the prior external-write rejection; save exact results locally.
