# Broader evidence-routing replay

Planned September 15 UTC / September 14 Denver. Compare all-static versus per-need routing with the same local community semantic search, hybrid rules and bounded keyword preparation reuse. Six first saved mixed-flow plans are retained in original manifest order, including the invalid yoga follow-up. Eight authored plans for existing development questions broaden billing, reports, caregiver/nanny access, booking, trash and negative controls. This is retrieval diagnosis, not new model understanding, unseen evaluation, human calibration or final-answer scoring.

Freeze fourteen cases, two methods and two repetitions. For the three valid live cases, add explicitly injected adapter failures: 68 total records. Refresh live evidence once through existing read-only connectors and preserve exact envelopes, source fetches and timestamps. Never silently shift a saved day to today or extend expiry. Saved dates that no longer match the community day yield a visible gap. Real adapter errors remain real errors; simulated errors are separately labeled. Compare static evidence across observed/failure scenarios, keep invalid plans and clarification cases out of retrieval, inspect actions and missing requested details. Source-presence scores cannot prove permission, report findings, a password or menu contents.

Use the current revalidated community snapshot, current local rules and exact existing embedding caches. No paid AI calls, downloads, subscriptions, resident submissions or release. Measure packet time separately from live refresh/model initialization. Preserve raw source/action/omission/diagnostic payloads. Adopt no production behavior from this replay. Notion synchronization remains pending the prior automatic approval review rejection of the external update.

## Captured result and decision

Capture 237b07de527036374c8d67d0ef9ffc584c7526d4 in artifacts/quality-eval/broad-retrieval-20260915 completed 68/68, process 19355 exited 0. All 34 method/scenario repeat pairs have identical complete responses. Four yoga records preserve the invalid first saved plan (missing need request and identity), four ambiguous-price records clarify without retrieval, and sixty records contain evidence packets. No final answers were generated or rated. Twenty-seven focused replay, live-source, community-identity and preparation tests pass.

Four read-only source fetches produced current food, recycling and pool evidence. All three observed live cases retained one operational source under both methods. Injected failures removed live evidence, kept the explicit failure diagnostic and preserved the static source payload exactly in all six method/case comparisons. Saved live dates still matched September 14 in Denver; no date rebasing or expiry extension occurred.

Reject the current per-need routing shortcut as a general solution. Its exclusive evidence-kind rule skips all governing documents for information and action needs. That saves time but loses relevant support:

- Nanny membership: both methods miss the approved caregiver application page. All-static nevertheless retrieves section 17-146's caregiver membership provision and section 13-2's caregiver conditions. Per-need removes these and leaves general membership, nonresident membership, mailbox and directory pages. Treating an ordinary information question as unable to need rules loses a direct answer.
- Great Hall booking: both retain the booking destination, but per-need drops section 17-188's reservation steps and section 17-197's facility-specific context. A destination-only score would miss this loss of useful process evidence. This finding does not independently certify every old fee or condition for a resident answer.
- Food/menu: the observed live projection proves the day's named schedule listing, explicitly not menu contents. The static menu search supplies navigation to Municode, programs, news and coffee. Per-need removes irrelevant vehicle/food rules but still supplies these unrelated links and no explicit static menu-gap diagnostic.
- Coffee password: per-need reduces the packet from 27,225 to 143 text characters, but all remaining sources are navigation only. No password is supported. Smaller packets do not establish an answer, and a coffee destination is not proof of a password.
- Recycling plus storage: core live pickup and storage rules survive, but per-need removes the approved trash concern form and recycling guidance alongside unrelated pet-waste maps. That optional-action loss must be evaluated for useful proactiveness, especially during live failure.

The six authored positive static cases retain their declared community target groups in 5/6 cases under both methods, with nanny access missing in both. This is a diagnostic source count only. It does not capture lost governing support, scope applicability or answer quality. The saved lighting follow-up retains the updated lighting rule under both methods; this does not repair the invalid yoga interpretation.

Local packet timing, excluding live refresh and model startup: all-static median 2,105 ms / sample p95 4,244 ms; per-need 574 / 2,923 ms, thirty valid retrievals per method. Model initialization 3,886 ms; process peak RSS 433,098,752 bytes (about 413 MiB). The faster method is not an adoption candidate because it loses useful evidence. Four source HTTP fetches, zero paid AI API calls, zero downloads and zero new subscriptions. Existing CPU/hosting cost and whole-answer latency remain unmeasured; there is no new monthly commitment.

Next investigate a shared plan/evidence contract that allows each resident need to require several source roles, rather than letting one label shut a source library off. Preserve permission/process evidence and useful next steps, distinguish navigation from factual support, and expose unsupported static needs without treating neighboring links as answers. Test any change against these captured losses as well as prior compound cases. Avoid fixing these with individual question keywords or adopting a vector database solely to change where the same vectors are stored. Paid model comparisons and blind human rating calibration remain pending; no production or verified-live readiness claim.

Reproduce the audit with `node scripts/quality-eval/summarize-broad-retrieval.js artifacts/quality-eval/broad-retrieval-20260915`. Raw plans, exact source snapshots, live envelopes, randomized design and every record remain preserved. comparison.json includes all selected source identities, actions, omissions, diagnostics and per-case timing. The next-step interpretation above is an engineering review, not human calibration.
